{
  "name": "agent-distro",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "agent-distro": "bin/agent-distro.mjs"
  },
  "files": [
    "bin",
    "dist"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "assets:generate": "node scripts/generate-catalog.mjs",
    "assets:check": "node scripts/generate-catalog.mjs --check",
    "build": "npm run assets:generate && tsdown src/agent-distro.ts --format esm --out-dir dist --copy assets",
    "lint": "biome ci",
    "lint:fix": "biome check --write",
    "prepare": "npm run build",
    "prepack": "npm run build",
    "test": "npm run build && npm run assets:check && vitest run --no-file-parallelism",
    "test:proof": "npm run build && node test/proof.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.6",
    "execa": "^9.6.1",
    "tsdown": "^0.22.14",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "commander": "^14.0.3"
  }
}
