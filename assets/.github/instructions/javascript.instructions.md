# JavaScript development

Use the repository's declared package manager and Node version. Inspect package scripts before changing dependencies, preserve lockfile consistency, and run the narrowest relevant typecheck, lint, and test commands.
