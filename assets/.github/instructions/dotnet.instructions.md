# .NET development

Use the repository's pinned .NET SDK and inspect solution and project files before changing dependencies. Keep package references consistent, run dotnet build and the smallest relevant dotnet test command, and report target-framework impact.
